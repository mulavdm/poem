# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

#cpp_sidecar build
cpp_sidecar/build/
cpp_sidecar/build-*/

# windows_host/build.ps1 packaging output (portable bundle, ZIP, MSIX)
/dist/

# android_engine/build_apk.sh output (per-ABI apkroot, .apk, .so, and the
# signed APK it writes next to the script)
android_engine/build/
*.apk
*.apk.idsig

# Go cache
.gocache/
# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# IDEs and Editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
*.swo
*~

# Rust build artifacts
target/

# OS-specific files
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini


# Project-specific Build artifacts and logs
/engine
/engine.exe
/poem_go.exe
/build/
/output/
crash.log
/cartography-wasm
