<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="__PACKAGE__"
    android:versionCode="1"
    android:versionName="0.1">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="36" />
  <!-- Predictive back (default at targetSdk 36) would finish the activity
       without delivering KEYCODE_BACK to onInputEvent; opting out lets the
       presenter translate back into Escape (dismiss overlay / clear focus). -->
  <application android:label="__LABEL__"
      android:hasCode="false"
      android:extractNativeLibs="true"
      android:enableOnBackInvokedCallback="false">
    <activity android:name="android.app.NativeActivity"
        android:exported="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:windowSoftInputMode="adjustResize"
        android:configChanges="orientation|screenSize|keyboardHidden">
      <meta-data android:name="android.app.lib_name" android:value="poemhost" />
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
